import {config,headers} from "./config";import {STATE_ID,emptyState,migrate} from "./state";
export async function loadState(){const {url}=config();const r=await fetch(`${url}/rest/v1/app_state?id=eq.${STATE_ID}&select=payload`,{headers:headers(),cache:"no-store"});if(!r.ok)throw new Error(`Supabase: ${await r.text()}`);const rows=await r.json();return migrate(rows?.[0]?.payload||emptyState())}
export async function saveState(state){const {url}=config();const r=await fetch(`${url}/rest/v1/app_state?on_conflict=id`,{method:"POST",headers:headers({Prefer:"resolution=merge-duplicates,return=minimal"}),body:JSON.stringify({id:STATE_ID,payload:migrate(state),updated_at:new Date().toISOString()})});if(!r.ok)throw new Error(`Supabase: ${await r.text()}`)}

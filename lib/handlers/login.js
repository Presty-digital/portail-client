import {NextResponse} from "next/server";import {loadState} from "@/lib/db";import {verifyPassword,signSession,cookieName} from "@/lib/security";
export async function POST(req){try{const {email,password}=await req.json();const s=await loadState();const u=s.users.find(x=>x.active!==false&&x.email?.toLowerCase()===String(email||"").toLowerCase());if(!u||!verifyPassword(password||"",u.passwordHash))return NextResponse.json({error:"Identifiants incorrects"},{status:401});const res=NextResponse.json({ok:true});res.cookies.set(cookieName(),signSession({userId:u.id,role:u.role,institutId:u.institutId||null,exp:Date.now()+604800000}),{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:604800});return res}catch(e){return NextResponse.json({error:e.message},{status:500})}}

import {cookies} from "next/headers";import {cookieName,parseSession} from "./security";
export async function getSession(){const c=await cookies();return parseSession(c.get(cookieName())?.value||"")}
export function allowedIds(session){return session?.role==="agency_admin"?null:(Array.isArray(session?.accessIds)&&session.accessIds.length?session.accessIds:(session?.institutId?[session.institutId]:[]));}
export function visibleState(s,session){if(session.role==="agency_admin")return s;const ids=allowedIds(session)||[];return{...s,users:[],instituts:s.instituts.filter(x=>ids.includes(x.id)),campaigns:s.campaigns.filter(x=>ids.includes(x.institutId)),leads:s.leads.filter(x=>ids.includes(x.institutId)),expenses:s.expenses.filter(x=>ids.includes(x.institutId)),reviews:s.reviews.filter(x=>ids.includes(x.institutId)),integrations:(s.integrations||[]).filter(x=>ids.includes(x.institutId))}}

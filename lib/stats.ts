export const euro=(n:any)=>`${Number(n||0).toLocaleString("fr-FR",{maximumFractionDigits:0})} €`;export const euros=euro;
const configFor=(state:any,iid:any)=>state.instituts?.find((i:any)=>i.id===iid)?.pipelineConfig||[];
const stageType=(state:any,iid:any,status:any)=>configFor(state,iid).find((s:any)=>s.label===status)?.type||({"RDV fixé":"appointment_booked","RDV réalisé":"appointment_completed","No-show":"no_show","Gagné":"won","Client converti":"won","Perdu":"lost"} as any)[status]||"prospect";
const result=(state:any,institutId:any,leads:any[],expenses:any[])=>{const spend=expenses.reduce((s:any,x:any)=>s+Number(x.amount||0),0);const conv=leads.filter((x:any)=>x.converted||stageType(state,institutId,x.statut)==='won');const revenue=conv.reduce((s:any,x:any)=>s+Number(x.value||x.valeurClient||0),0);const rdv=leads.filter((x:any)=>['appointment_booked','appointment_completed','no_show','won'].includes(stageType(state,institutId,x.statut))||x.rdvAt||x.creneauRdv).length;const noShow=leads.filter((x:any)=>stageType(state,institutId,x.statut)==='no_show').length;return{leads:leads.length,rdv,noShow,converted:conv.length,revenue,spend,cpl:leads.length?spend/leads.length:0,cac:conv.length?spend/conv.length:0,roi:spend?revenue/spend:0,conversion:leads.length?conv.length/leads.length*100:0}};
export function metrics(state:any,institutId:any,period:string){const own=(x:any)=>!institutId||x.institutId===institutId;const leads=state.leads.filter((x:any)=>own(x)&&(x.dateContact||x.date||"").slice(0,7)===period);const expenses=state.expenses.filter((x:any)=>own(x)&&`${x.year}-${String(x.month).padStart(2,"0")}`===period);return result(state,institutId,leads,expenses)}
export function metricsRange(state:any,institutId:any,start:string,end:string,all=false){const own=(x:any)=>!institutId||x.institutId===institutId;const inRange=(v:any)=>{if(all)return true;const d=String(v||'').slice(0,10);return Boolean(d)&&d>=start&&d<=end};const leads=state.leads.filter((x:any)=>own(x)&&inRange(x.dateContact||x.date));const expenses=state.expenses.filter((x:any)=>{if(!own(x))return false;if(all)return true;const m=`${x.year}-${String(x.month).padStart(2,'0')}`;const first=`${m}-01`;const last=new Date(Number(x.year),Number(x.month),0).toISOString().slice(0,10);return first<=end&&last>=start});return result(state,institutId,leads,expenses)}
export const computePerformance=(leads:any[]=[],expenses:any[]=[])=>{const state:any={leads,expenses,instituts:[]};const period=(leads[0]?.dateContact||new Date().toISOString()).slice(0,7);return metrics(state,null,period)};

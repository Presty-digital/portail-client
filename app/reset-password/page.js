"use client";
import {useEffect,useState} from "react";
export default function ResetPassword(){const[token,setToken]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[message,setMessage]=useState("");useEffect(()=>{const h=new URLSearchParams(location.hash.replace(/^#/,""));setToken(h.get("access_token")||"")},[]);async function submit(){setError("");if(password.length<8)return setError("Le mot de passe doit contenir au moins 8 caractères.");if(password!==confirm)return setError("Les deux mots de passe ne correspondent pas.");const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({accessToken:token,password})});const j=await r.json().catch(()=>({}));if(!r.ok)return setError(j.error||'Impossible de modifier le mot de passe.');setMessage('Votre mot de passe a été modifié. Vous pouvez vous reconnecter.')}return <div className="auth-page reset-only"><div className="auth-form-panel"><div className="auth-form-wrap"><img className="presty-logo" src="/presty-logo.png" alt="Presty"/><span className="eyebrow">SÉCURITÉ</span><h2>Nouveau mot de passe</h2><p className="auth-intro">Choisissez un mot de passe d’au moins 8 caractères.</p>{!token&&<div className="alert error">Lien de réinitialisation invalide ou expiré.</div>}<label className="field"><span>Nouveau mot de passe</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="field"><span>Confirmer le mot de passe</span><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="alert error">{error}</div>}{message&&<div className="alert success">{message}</div>}<button className="primary full" disabled={!token} onClick={submit}>Enregistrer le nouveau mot de passe</button><div className="auth-links"><a href="/login">← Retour à la connexion</a></div></div></div></div>}

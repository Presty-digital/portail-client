'use client';
import { useEffect, useState } from 'react'; import { useRouter } from 'next/navigation'; import Brand from '@/components/Brand'; import { browserSupabase } from '@/lib/supabase';
export default function Login(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const router=useRouter();
 useEffect(()=>{fetch('/api/setup').then(r=>r.json()).then(j=>{if(!j.initialized) router.replace('/initialisation')}).catch(()=>{});},[router]);
 async function submit(e){e.preventDefault();setLoading(true);setError('');const {error}=await browserSupabase().auth.signInWithPassword({email,password}); if(error){setError('Identifiants incorrects.');setLoading(false);return;} router.push('/portail');}
 return <main className="authPage"><section className="authVisual"><Brand/><div className="heroCopy"><span className="eyebrow">PORTAIL PRESTY</span><h1>Pilotez votre croissance.<br/>Sans complexité.</h1><p>Prospects, rendez-vous, chiffre d’affaires et rentabilité réunis dans un espace clair.</p></div><div className="visualCard"><div><span>ROI moyen</span><strong>x5,2</strong></div><div className="miniBars"><i/><i/><i/><i/><i/></div></div></section><section className="authPanel"><div className="loginBox"><h2>Bon retour parmi nous</h2><p>Connectez-vous à votre espace sécurisé.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Mot de passe<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading}>{loading?'Connexion…':'Se connecter'}</button></form></div></section></main> }

'use client';
import{useEffect,useState}from'react';import{useRouter}from'next/navigation';import{CheckCircle2}from'lucide-react';
export default function Setup(){const router=useRouter();const[name,setName]=useState('Dylan');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[done,setDone]=useState(false);useEffect(()=>{fetch('/api/setup').then(r=>r.json()).then(j=>{if(!j.needsSetup)router.replace('/login')})},[router]);async function submit(e:React.FormEvent){e.preventDefault();setError('');const r=await fetch('/api/setup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const j=await r.json();if(!r.ok){setError(j.error||'Erreur');return}setDone(true);setTimeout(()=>router.replace('/login'),1400)}return <div className="setup-page"><div className="setup-card">{done?<div className="success"><CheckCircle2 size={48}/><h1>Compte administrateur créé</h1><p>Redirection vers la connexion…</p></div>:<form onSubmit={submit}><span className="eyebrow">Première configuration</span><h1>Créer le compte Presty</h1><p>Cette étape n’apparaît qu’une seule fois.</p><label>Votre nom<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Email administrateur<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Mot de passe<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button>Créer mon espace administrateur</button></form>}</div></div>}

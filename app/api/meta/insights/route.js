import {NextResponse} from 'next/server';import {metaFetch,requireMetaAccess,leadCount} from '@/lib/meta';
export const dynamic='force-dynamic';
export async function GET(req){try{const q=new URL(req.url).searchParams,institutId=q.get('institutId')||'',accountId=q.get('accountId')||'',since=q.get('since')||'',until=q.get('until')||'';await requireMetaAccess(institutId);if(!accountId)return NextResponse.json({error:'Compte publicitaire manquant'},{status:400});const act=accountId.startsWith('act_')?accountId:`act_${accountId}`;const time_range=since&&until?JSON.stringify({since,until}):undefined;const j=await metaFetch(`${act}/insights`,{fields:'campaign_id,campaign_name,spend,impressions,reach,clicks,inline_link_clicks,actions,cpm,cpc,ctr',level:'campaign',time_range,date_preset:time_range?undefined:'this_month',limit:500});const rows=(j.data||[]).map(x=>{const leads=leadCount(x.actions);const spend=Number(x.spend||0);return {campaignId:x.campaign_id,campaignName:x.campaign_name,spend,impressions:Number(x.impressions||0),reach:Number(x.reach||0),clicks:Number(x.clicks||0),linkClicks:Number(x.inline_link_clicks||0),leads,cpl:leads?spend/leads:0,cpm:Number(x.cpm||0),cpc:Number(x.cpc||0),ctr:Number(x.ctr||0)};});const totals=rows.reduce((a,x)=>({spend:a.spend+x.spend,impressions:a.impressions+x.impressions,reach:a.reach+x.reach,clicks:a.clicks+x.clicks,linkClicks:a.linkClicks+x.linkClicks,leads:a.leads+x.leads}),{spend:0,impressions:0,reach:0,clicks:0,linkClicks:0,leads:0});totals.cpl=totals.leads?totals.spend/totals.leads:0;return NextResponse.json({rows,totals});}catch(e){return NextResponse.json({error:e.message},{status:e.status||500})}}

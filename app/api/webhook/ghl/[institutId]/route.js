import {NextResponse} from "next/server";import {loadState,saveState} from "@/lib/db";import {uid,today} from "@/lib/state";export async function POST(req,{params}){if(req.headers.get("x-webhook-secret")!==process.env.GHL_WEBHOOK_SECRET)return NextResponse.json({error:"Unauthorized"},{status:401});const {institutId}=await params,b=await req.json(),s=await loadState();s.leads.push({id:uid(),institutId,dateContact:today(),prenom:b.first_name||b.prenom||"",nom:b.last_name||b.nom||"",telephone:b.phone||"",email:b.email||"",category:b.category||"Autres",typeSoin:b.type_soin||"",problematique:b.problematique||"",statut:"Non qualifié",converted:false,value:0});await saveState(s);return NextResponse.json({ok:true})}

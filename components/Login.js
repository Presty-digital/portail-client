'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {signIn} from '@/lib/client';import {ArrowRight,LockKeyhole} from '@/components/Icons';
export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setError('');try{await signIn(email,password);r.push('/portail')}catch(e){setError(e.message)}finally{setLoading(false)}}return <main className="authPage"><section className="authVisual"><div className="brandMark"><span>P</span>PRESTY</div><div><p className="eyebrow">PORTAIL CLIENT</p><h1>Transformez les données en décisions.</h1><p>Suivez vos campagnes, vos prospects et votre chiffre d’affaires depuis une interface unique.</p></div><div className="visualStats"><div><b>ROI</b><span>en temps réel</span></div><div><b>CRM</b><span>multi-appareils</span></div></div></section><section className="authPanel"><form className="authCard" onSubmit={submit}><div className="iconBubble"><LockKeyhole size={22}/></div><h2>Connexion</h2><p>Accédez à votre espace sécurisé.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Mot de passe<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading}>{loading?'Connexion…':'Se connecter'}<ArrowRight size={18}/></button><a href="/initialisation" className="setupLink">Première installation administrateur</a></form></section></main>}

'use client';
import{useState}from'react';import{api}from'@/lib/client-api';import type{Campaign,Category}from'@/lib/types';
export default function AdminExpenseForm({institutId,campaigns,month,year,onSaved}:{institutId:string;campaigns:Campaign[];month:number;year:number;onSaved:()=>void}){const[category,setCategory]=useState<Category>('Minceur');const[campaignName,setCampaignName]=useState('');const[amount,setAmount]=useState('');const[saving,setSaving]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);await api('/api/expenses',{method:'POST',body:JSON.stringify({institutId,category,campaignName,amount:Number(amount),month,year})});setCampaignName('');setAmount('');setSaving(false);onSaved()}return <form className="expense-form" onSubmit={submit}><div><span className="eyebrow">Presty uniquement</span><h3>Ajouter une dépense</h3></div><select value={category} onChange={e=>setCategory(e.target.value as Category)}><option>Minceur</option><option>Visage</option><option>Épilation</option><option>Autres</option></select><input placeholder="Soin ou campagne" value={campaignName} onChange={e=>setCampaignName(e.target.value)} required/><input type="number" min="0" step="0.01" placeholder="Montant €" value={amount} onChange={e=>setAmount(e.target.value)} required/><button className="primary" disabled={saving}>{saving?'Enregistrement…':'Enregistrer'}</button></form>}

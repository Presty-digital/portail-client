'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {ShieldCheck} from '@/components/Icons';
export default function Initialisation(){const r=useRouter(),[f,setF]=useState({name:'Dylan',email:'',password:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);const res=await fetch('/api/initialize',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await res.json();setLoading(false);if(!res.ok)setError(j.error||'Erreur');else r.push('/login')}return <main className="centerPage"><form className="setupCard" onSubmit={submit}><div className="iconBubble"><ShieldCheck/></div><p className="eyebrow">INITIALISATION UNIQUE</p><h1>Créer le compte administrateur Presty</h1><p>Ce compte aura une visibilité complète et pourra créer les accès de tous les instituts.</p><label>Nom<input value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email administrateur<input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/></label><label>Mot de passe<input type="password" minLength="8" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading}>{loading?'Création…':'Créer mon compte administrateur'}</button></form></main>}
